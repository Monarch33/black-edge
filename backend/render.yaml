services:
  - type: web
    name: black-edge-backend
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: ./
    plan: free
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: ENVIRONMENT
        value: production
      - key: PORT
        value: 8000
      - key: POLYGON_RPC_URL
        sync: false
      - key: STRIPE_SECRET_KEY
        sync: false
