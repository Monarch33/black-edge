# Black Edge — The Invisible Engine
# ================================
# Copy to .env.local and fill ALL values.
# App CRASHES at startup if any required key is missing.

# ── REQUIRED ───────────────────────────────────────────────────
DATABASE_URL=postgresql://user:password@localhost:5432/blackedge
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxx
FERNET_KEY=your_base64_fernet_key_here  # python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"

# LLM — at least one required
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxx
# OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxx

# ── Optional (Polymarket admin read-only) ──────────────────────
POLYMARKET_API_URL=https://gamma-api.polymarket.com
POLYMARKET_CLOB_URL=https://clob.polymarket.com
POLYMARKET_ADMIN_KEY=

# Polygon RPC
POLYGON_RPC_URL=https://polygon-rpc.com
POLYGON_WS_URL=wss://polygon-bor-rpc.publicnode.com

# Redis
REDIS_URL=redis://localhost:6379

# Firebase (for auth)
FIREBASE_CREDENTIALS_PATH=

# Security
JWT_SECRET_KEY=change_me_in_production
CORS_ORIGINS=http://localhost:3000,https://blackedge.io

# Email (Resend)
RESEND_API_KEY=
